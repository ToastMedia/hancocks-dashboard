# clasp — local credentials and the real project config (contains scriptId).
# The repo is PUBLIC, so we commit .clasp.json.example instead and keep the
# real .clasp.json untracked.
.clasp.json
.clasprc.json

# OS / editor noise
.DS_Store
